pipeline {
    agent any 
    stages {

        stage('Build') { 
            steps {
                sh '''
                echo "************ Containerizing app to docker image **********"
                '''
            }
        }

        stage('Deploy') { 
            steps {
                sh '''
                echo "************ Deploy app to Docker Hub **********"
                echo "************ Deploy app to Docker Hub **********"
                '''
            }
        }

    }
}